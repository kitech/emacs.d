;; restore el-get packages

(defvar el-get-pkgs '(
		      cedet  company-mode  ecb  el-get
			     php-mode  yasnippet  yasnippets
		      ))
(defun restore-el-get-packages ()
  ())

(provide 'restore)
